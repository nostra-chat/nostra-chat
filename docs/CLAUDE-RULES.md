# Moved: Nostra.chat subsystem rules

The shared subsystem invariants formerly stored here now live in
[`SUBSYSTEM-RULES.md`](SUBSYSTEM-RULES.md). This compatibility pointer is kept
for historical plans and external links; do not add new rules to this file.
